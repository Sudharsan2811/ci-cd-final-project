apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: cleanup
spec:
  steps:
    - name: cleanup
      image: python:3.11
      script: |
        #!/bin/sh
        rm -rf __pycache__
        rm -rf .pytest_cache

---
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: test
spec:
  steps:
    - name: nose-test
      image: python:3.11
      script: |
        #!/bin/sh
        pip install -r requirements.txt
        pip install nose
        nosetests
